interface:
  display_name: "Optimize GPT-5.6 Prompt"
  short_description: "Optimize GPT-5.6 prompts without losing context"
  default_prompt: "Use $optimize-gpt-5-6-prompt to optimize this GPT-5.6 prompt or follow-up while preserving the active task contract."
